# ============================================================================
# Core Computer Vision Libraries
# ============================================================================

# OpenCV contrib — supports NumPy 2.x
opencv-contrib-python==4.10.0.84

# MediaPipe - Google's pre-trained ML library for hand tracking
mediapipe==0.10.32

numpy>=2

# ============================================================================
# Web Backend
# ============================================================================

# FastAPI — the web framework
fastapi==0.111.0

# Uvicorn — the ASGI server that runs FastAPI (includes WebSocket support)
uvicorn[standard]==0.29.0